'use strict';

const fizzBuzzTree = function(node.val) {
  if (node.val % 3 && node.val % 5 === 0) {
    node.val = FizzBuzz;
  }
  if (node.val % 3 === 0) {
    node.val = Fizz;
  }
  if (node.val % 5 === 0) {
    node.val = Buzz;
  }
  return this;
}

